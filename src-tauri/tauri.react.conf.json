{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "bun run react:dev",
    "devUrl": "http://127.0.0.1:1421",
    "beforeBuildCommand": "bun run build:backend && bun run react:build && bun scripts/stage-vendor-bundle.mjs && bun scripts/stage-runtime-sidecar.mjs && bun scripts/write-app-bundle-manifest.mjs",
    "frontendDist": "../dist/renderer"
  },
  "app": {
    "security": {
      "capabilities": [
        "default",
        "dev-server"
      ]
    }
  }
}
