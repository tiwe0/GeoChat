import { Box } from "@mui/material";

/** Reserved settings surface for the upcoming Skills module. */
export function SkillsSettings() {
  return <Box className="settings-page settings-skills-page" />;
}
