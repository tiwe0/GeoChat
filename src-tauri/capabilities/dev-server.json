{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "dev-server",
  "description": "Grants shell permissions to the Vite dev server origin. Tauri treats a devUrl as remote content, so without this the window cannot be dragged and desktop commands are rejected during development. Excluded from release builds by app.security.capabilities in tauri.conf.json.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:1421"
    ]
  },
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "opener:default"
  ]
}
